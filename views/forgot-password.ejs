<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/global.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title><%= pageTitle %></title>
  </head>
  <body style="background-color: lightblue">
    <%- include('header') -%>

    <!-- Main Content -->
    <form
      action="/forgot-password"
      method="POST"
      class="container my-auto w-50"
    >
      <h2>Forgot Password</h2>
      <div class="form-floating mt-4">
        <input
          class="form-control"
          id="email"
          name="email"
          aria-describedby="email-help"
        />
        <label for="email" class="form-label">Email address</label>
        <% if (locals.error) { %>
        <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>
        <div id="email-help" class="form-text mt-4">
          <p>We'll send you a link to reset your password.</p>
          <p>(Just kidding, it will appear in the console)</p>
          <p>
            (Just kidding, the password is encrypted, so it is not stored in the
            database)
          </p>
        </div>
        <% if (locals.data) { %>
        <div class="alert alert-success" role="alert">
          <p>Salt: <%= locals.data.salt %></p>
          <p>Hash: <%= locals.data.hash %></p>
        </div>
        <% } %>
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

    <%- include('footer') -%>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
